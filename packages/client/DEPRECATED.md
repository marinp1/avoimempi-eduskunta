# DEPRECATED

This package (`packages/client`) is deprecated and will be removed once `packages/webapp` reaches feature parity.

**Do not add new features here.**  All new frontend work goes in `packages/webapp/`.

The old stack (React 19 + Material-UI + Emotion) is being replaced with a new htmx-based UI in `packages/webapp/`.
