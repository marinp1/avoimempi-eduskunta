# Admin panel usage issues

At the moment it is impossible to quickly parse ALL fetched data easily.
Clicking parse at one table triggers reload which makes it extremely slow to sync database.

Similarly when table fetch is complete it triggers a reload of the page which makes fetching data slow.
