---
DONE
---

# VaskiData migration is different

At the moment VaskiData is migrated differently. It should not require pre-processing the parsed VaskiData JSON files into directory structure (in /vaski-data.) Instead the migrator should follow the same pattern as the rest of the migrations, meaning that import default processor and run at correct place.
